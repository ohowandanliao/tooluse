[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "schema-reuse"
version = "0.1.0"
description = "Pilot infrastructure for schema-reusable function-calling experiments."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
